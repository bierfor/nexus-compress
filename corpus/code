//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
//! Auto-generated synthetic source.
use std::collections::HashMap;

pub fn process(input: &[u8]) -> HashMap<String, usize> {
    let mut counts: HashMap<u8, usize> = HashMap::new();
    for &b in input {
        *counts.entry(b).or_insert(0) += 1;
    }
    let mut out: HashMap<String, usize> = HashMap::new();
    for (k, v) in counts.iter() {
        out.insert(format!("byte_{:02x}", k), *v);
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn empty() {
        assert!(process(b"").is_empty());
    }
}
